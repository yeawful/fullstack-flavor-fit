mutation createOrder($input: OrderCreateInput!) {
  createOrder(input: $input) {
    id
    orderId
    total
    status
    createdAt
    items {
      id
      quantity
      price
      ingredient {
        id
        name
        price
        iconUrl
      }
    }
  }
}
