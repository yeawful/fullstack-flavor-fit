mutation AddNewCommemt($input: CommentCreateInput!) {
  createComment(input: $input) {
    id
    content
    createdAt
    author {
      avatarUrl
      profile {
        fullName
      }
    }
  }
}
